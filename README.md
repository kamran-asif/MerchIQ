# MerchIQ

This repository contains the MerchIQ fullstack application (backend and frontend).

Contents:
- `backend/` - FastAPI backend
- `frontend/` - Vite + React frontend

To push this repo to GitHub, add a remote and run `git push` with your preferred authentication method.
